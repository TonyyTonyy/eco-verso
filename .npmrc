# Este projeto não faz parte do diretório-monorepo acidental em Downloads —
# impede o pnpm de subir diretórios à procura de uma raiz de workspace.
ignore-workspace-root-check=true
link-workspace-packages=false
shared-workspace-lockfile=false
